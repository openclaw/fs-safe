[workspace]
members = ["native", "archive-core", "archive-wasm"]
resolver = "2"

[profile.release]
lto = true
# Cargo's Mach-O symbol stripping can corrupt the link-edit string table after
# static compression libraries are linked. Platform package assembly may strip
# with the target toolchain after the loadable artifact has been validated.
strip = false
